include ':app'
include ':local_ai_runtime'

// Tauri creates this ignored file for app builds. Credential contract tests run
// Gradle directly and do not need its generated project wiring.
if (file('tauri.settings.gradle').exists()) {
    apply from: 'tauri.settings.gradle'
}
